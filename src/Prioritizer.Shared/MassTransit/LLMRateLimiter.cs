namespace Prioritizer.Shared.MassTransit;

public static class LLMRateLimiter
{
    // Allow 5 calls every 1 second (5 executions per 1s time window)
    ///public static readonly Policy RateLimitPolicy = ..
}

